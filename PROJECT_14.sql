SELECT USER_ID, MIN(CREATED_DATE) AS FirstOrderDate FROM SALES_PROJECT GROUP BY USER_ID;
-- IT DISPLAYS THE FIRST ORDER PURCHASED BY EACH CUSTOMER FROM SALES_PROJECT TABLE WHERE AGGREGATE FUNCTION MIN IS USED